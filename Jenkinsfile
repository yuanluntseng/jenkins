pipeline {
    agent any // or specify a label for a specific agent

    stages {
        stage('Build') {
            steps {
                bat 'build.sh' // Executes build.bat
            }
        }
    }
}
